<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:background="@drawable/background"
    android:layout_width="match_parent"
    android:layout_height="match_parent" >
    
    <ImageView 
        android:id="@+id/last_pic_iv"
        android:background="@null"
        android:scaleType="fitXY"
        android:layout_width="match_parent"
    	android:layout_height="match_parent"/>
    
    
    
    <ImageButton 
        android:id="@+id/ensure_video_ibtn"
        android:src="@drawable/finish_video_icon"
        android:background="@null"
        android:padding="10dp"
        android:layout_above="@+id/cancel_ensure_ibtn"
        android:layout_alignParentRight="true"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"/>
    
    <ImageButton 
        android:id="@+id/cancel_ensure_ibtn"
        android:src="@drawable/cancel_video_ensure_icon"
        android:background="@null"
        android:padding="30dp"
        android:layout_centerHorizontal="true"
        android:layout_alignParentBottom="true"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"/>

</RelativeLayout>
